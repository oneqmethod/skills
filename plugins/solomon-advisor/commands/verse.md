---
description: חיפוש בבנק הפסוקים של שלושת ספרי שלמה לפי נושא עסקי
argument-hint: [נושא — חוב, עיתוי, בידול, סיכון, פיזור...]
---

חפש בבנק הפסוקים של שלמה בנושא: **$ARGUMENTS**

הרץ:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/skills/solomon-decision/scripts/verses.sh "$ARGUMENTS"
```

ללא ארגומנט — הצג את רשימת התגיות הזמינות עם `--tags`.

הצג את הפסוקים שנמצאו עם התרגום העסקי ושאלת הבדיקה של כל אחד, וסכם בשורה אחת
מה שלושת הספרים אומרים יחד על הנושא. אם היציאה היא בקוד 3 — אין פסוק לנושא הזה
בבנק; אמור זאת במפורש ואל תמציא תחליף.
